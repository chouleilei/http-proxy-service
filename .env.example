# HTTP代理服务 v2.0 Docker部署环境变量配置示例
# 复制此文件为 .env 并填入实际值

# ===========================================
# 必需配置 - 必须修改为您的实际值
# ===========================================

# 上游代理API地址 (必填)
UPSTREAM_PROXY_URL=https://api.doudouzi.me/api/proxy

# 代理服务器1认证信息 (必填)
PROXY1_USERNAME=your-username
PROXY1_PASSWORD=your-password

# 代理服务器2认证密码 (必填)
PROXY2_PASSWORD=your-password

# ===========================================
# 可选配置 - 可以保持默认值
# ===========================================

# 服务端口配置
API_PORT=5237
PROXY1_PORT=5238
PROXY2_PORT=5239
HTTPS_PROXY2_PORT=5240
HTTPS_API_PORT=5241
HOST=0.0.0.0

# 代理池配置
MAX_POOL_SIZE=10
REFRESH_INTERVAL=300000
HEALTH_CHECK_INTERVAL=600000
CONCURRENT_HEALTH_CHECKS=3
TEST_TIMEOUT=4000
TEST_URL=http://httpbin.org/ip

# 日志配置
LOG_LEVEL=info
LOG_FILE=logs/proxy.log
LOG_MAX_SIZE=10m
LOG_MAX_FILES=1d
LOG_DATE_PATTERN=YYYY-MM-DD

# 监控配置
MONITORING_ENABLED=true
METRICS_INTERVAL=30000
HISTORY_SIZE=100

# SSL配置 (可选)
SSL_ENABLED=false
SSL_KEY_PATH=/path/to/privkey.pem
SSL_CERT_PATH=/path/to/fullchain.pem

# 性能配置
REQUEST_TIMEOUT=30000
MAX_REDIRECTS=5
KEEP_ALIVE=true

# Node.js环境
NODE_ENV=production

# ===========================================
# Docker部署说明
# ===========================================
# 1. 复制此文件为 .env
# 2. 修改上述必需配置项为您的实际值
# 3. 根据需要调整可选配置项
# 4. 运行: docker-compose -f docker-compose-standalone.yml up -d
# 5. 访问: http://localhost:5237 查看管理界面
# 6. 测试代理: curl -x http://username:password@localhost:5238 http://httpbin.org/ip